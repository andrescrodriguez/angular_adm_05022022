export function capitalizarPrimeraLetra(valor: string): string {
    if(!valor) return '';
    if(valor.length === 0) return '';
    //  const valorArr = valor.trim().split(' ');
     console.log(valor);
    //  valorArr[0] = valor[0].charAt(0).toUpperCase();
    //  valor = valorArr.join();
    
     return valor;
}